import { Page } from "../../components/layout/Page";

export const NotFoundPage = () => {
    return (
        <Page>
            <div style={{display: 'flex', fontWeight: 'bolder', fontSize: '10rem', color: 'red', marginLeft: '30rem', padding: '10rem'}}>
             NOT FOUND
            </div>
        </Page>
        
    );
}